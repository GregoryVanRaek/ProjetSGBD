<Page x:Class="LocationVoiture.WPF.View.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LocationVoiture.WPF.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomeView">
    <Grid>
        <TextBlock Text="Welcome to your rental management system" FontSize="20" HorizontalAlignment="Center" Margin="10" />
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Client" Command="{Binding NavigateToClientViewCommand}" Margin="5" />
            <Button Content="Rentals" Command="{Binding NavigateToRentalViewCommand}" Margin="5" />
            <Button Content="Car management" Command="{Binding NavigateToCarViewCommand}" Margin="5" />
            <Button Content="Exit" Margin="5" />
        </StackPanel>
    </Grid>
</Page>
